<p id="notice"><%= notice %></p>

<p>
  <strong>名称:</strong>
  <a class="text-info"><%= @dictionary.name %></a>
</p>

<p>
  <strong>詳細:</strong>
  <a class="text-info"><%= @dictionary.notes %></a>
</p>

<p>
  <strong>画像:</strong>
  <%= image_tag(@dictionary.image.to_s, size: "100x100") %>
</p>

<%= link_to 'Edit', edit_dictionary_path(@dictionary) %> |
<%= link_to 'Back', dictionaries_path %>
